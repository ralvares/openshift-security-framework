apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all
spec:
  podSelector: {}        # Applies to all pods in the namespace
  policyTypes:
  - Ingress
  - Egress
  ingress: []            # No allowed ingress → deny everything
  egress: []             # No allowed egress → deny everything