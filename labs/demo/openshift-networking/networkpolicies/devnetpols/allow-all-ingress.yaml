apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-all
spec:
  podSelector: {}          # Selects all pods in the namespace
  policyTypes:
  - Ingress
  ingress:
  - {}                     # Allow from anywhere
